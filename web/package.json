{
  "name": "hotloop-flow-editor",
  "version": "0.1.0",
  "private": true,
  "description": "HotLoop Flow editor. Vanilla TypeScript and native SVG — no jQuery, no D3, no framework.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
