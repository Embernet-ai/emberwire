{
  "name": "emberwire-editor",
  "version": "0.1.0",
  "private": true,
  "description": "Emberwire editor. Vanilla TypeScript and native SVG — no jQuery, no D3, no framework.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
